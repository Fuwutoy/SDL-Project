Create folder named "assets", "lib" and "inc"

put SDL2 and SDL2__ttf in lib and inc.

add any font in "assets" and change the code to support that font.

use "gcc main.c -Llib -lSDL2 -lSDL2_ttf -o main" to compile using GNU Compiler Collection.
